@model IEnumerable<Proyecto.ConsumoAgua>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PromedioConsumoAgua)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsumoActualAgua)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeriodoConsumo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clientes.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PromedioConsumoAgua)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsumoActualAgua)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PeriodoConsumo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clientes.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Clientes.Cedula }) |
            @Html.ActionLink("Details", "Details", new { id = item.Clientes.Cedula }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Clientes.Cedula })
        </td>
    </tr>
}

</table>
