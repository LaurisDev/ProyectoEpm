@model IEnumerable<Proyecto.ConsumoEnergia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MetaAhorroEnergia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsumoActualEnergia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeriodoConsumo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clientes.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MetaAhorroEnergia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsumoActualEnergia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PeriodoConsumo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clientes.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Clientes.Cedula }) |
            @Html.ActionLink("Details", "Details", new { id = item.Cedula }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Cedula })
        </td>
    </tr>
}

</table>
