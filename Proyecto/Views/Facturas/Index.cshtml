@model IEnumerable<Proyecto.Factura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValorPagar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValorPagar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Factura }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Factura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Factura })
        </td>
    </tr>
}

</table>
